using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinBehavior : MonoBehaviour
{
    //CoinMagnect() foi desativado pois ainda não é compativel com o InfinityPowerUpGenerator

    //GameObject powerUpBehavior;

    //Rigidbody2D rb;

    //Vector3 targetPosition;

    //float magnectSpeed = 5f;
    //private void Start()
    //{
    //    powerUpBehavior = GameObject.FindGameObjectWithTag("PowerUpMaster");
    //    rb = GetComponent<Rigidbody2D>();
    //}

    //void FixedUpdate()
    //{
    //    if (powerUpBehavior.GetComponent<PowerUpBehavior>().isPowered)
    //    {
    //        Vector2 targetDirection = (targetPosition - transform.position).normalized;
    //        rb.velocity = new Vector2(targetDirection.x, targetDirection.y) * magnectSpeed;
    //    }
    //}

    //public void SetTargetPosition(Vector3 position)
    //{
    //    targetPosition = position;
    //}
}
